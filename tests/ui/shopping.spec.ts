import {test} from '../../fixtures/testFixtures'; import {products,users} from '../../utils/testData'; import {CartPage} from '../../pages/CartPage';
test('@smoke user can add a product to the cart',async({loginPage,productsPage})=>{await loginPage.navigate();await loginPage.login(users.valid.username,users.valid.password);await productsPage.addProductByName(products.backpack);await productsPage.openCart();await new CartPage(loginPage.page).assertContainsProduct(products.backpack);});
test('@regression user can add multiple products',async({loginPage,productsPage})=>{await loginPage.navigate();await loginPage.login(users.valid.username,users.valid.password);await productsPage.addProductByName(products.backpack);await productsPage.addProductByName(products.bikeLight);await productsPage.openCart();const c=new CartPage(loginPage.page);await c.assertContainsProduct(products.backpack);await c.assertContainsProduct(products.bikeLight);});
